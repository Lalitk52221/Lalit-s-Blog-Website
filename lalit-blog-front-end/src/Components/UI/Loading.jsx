import React from "react";
import Loader from "../../../src/assets/Loader.gif";

const Loading = () => {
  return (
    <div>
      <img src={Loader} alt="" width={30} height={30} />
    </div>
  );
};

export default Loading;
